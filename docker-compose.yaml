version: '3i'
services:
  node-serveur:
    build:
      context: ./node-serveur/
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    depends_on:
      - mongo
    networks:
      - app-network

  mongo:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongodata:/data/db
    networks:
      - app-network

networks:
  app-network:

volumes:
  mongodata:
